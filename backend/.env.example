# Port the API server listens on
PORT=4000

# Allowed origin for CORS (frontend URL)
CORS_ORIGIN=http://localhost:5173

# SQLite database file location (Prisma datasource)
DATABASE_URL="file:./dev.db"
